# logan-cli

Terminal coding agent (Rust) built on xAI's Grok models — a fork of xAI's open-source Grok Build. **Competing coding-agent CLI, not a tool that complements Claude Code** — kept for reference/curiosity only. Needs its own `XAI_API_KEY`.

- **Source:** [hoodini/logan-cli](https://github.com/hoodini/logan-cli) (vendored)
- **Run:** `cd logan-cli && cargo build --release`

## Notes
_(empty)_
