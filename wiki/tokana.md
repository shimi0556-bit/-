# tokana

Local Claude Code token-usage analyzer + statusline — reads Claude Code's own usage receipts, no API key needed.

- **Source:** [hoodini/tokana](https://github.com/hoodini/tokana) (vendored)
- **Run:** `cd tokana && bun install && bun run dev` (indexes usage, then serves the dashboard)

## Notes
_(empty)_
