{
  "name": "shimotron",
  "private": true,
  "version": "1.0.0",
  "description": "Shimotron — a browser 3D engine with PBR, dynamic sky, terrain, water, physics, particles, an in-browser editor, and Shimotron Rally (a racing game built on it)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --mode race",
    "build:engine": "vite build",
    "build:race": "vite build --mode race",
    "usb": "vite build && vite build --mode race && node tools/bake-ground.mjs && vite build --mode usb && node tools/usb.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "cannon-es": "^0.20.0",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "vite": "^8.3.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
